# @cc [owner:ghuntley,label:build] environment-pinned-shell
# Directory activation MUST use devenv so Mix commands resolve the repository's
# pinned Elixir/OTP runtime rather than an arbitrary host installation.
eval "$(devenv direnvrc)"
use devenv
